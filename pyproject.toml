[project]
name = "queuebert"
version = "0.1.0"
description = "Async file processing service built with FastAPI"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.135.1",
    "uvicorn[standard]>=0.42.0",
]
